pipeline {
    agent any

    tools {
        maven 'Default'
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'master', url: 'https://github.com/ugileeknathn/selenium-automation.git'
            }
        }

        stage('Build') {
            steps {
                dir('selenium-automation') {
                    bat 'mvn clean install'
                }
            }
        }

        stage('Run Tests') {
            steps {
                dir('selenium-automation') {
                    bat 'mvn test'
                }
            }
        }

        stage('Publish Test Results') {
            steps {
                // Collect JUnit XML reports generated by Maven Surefire
                junit 'selenium-automation/target/surefire-reports/*.xml'
            }
        }
    }

    post {
        success {
            echo ' Build and tests completed successfully!'
        }
        failure {
            echo ' Build failed. Check console output for details.'
        }
    }
}
